'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

require('./addProcess.js');
var vue = require('vue');
var districtCluster = require('@vuemap/district-cluster');
var vueAmapUtil = require('@vuemap/vue-amap-util');

var script = vue.defineComponent({
  name: "ElAmapLayerDistrictCluster",
  mixins: [vueAmapUtil.registerMixin],
  props: {
    data: {
      required: true,
      type: Array
    },
    // 数据源数组，每个元素即为点相关的信息
    getPosition: {
      type: Function
    },
    // 返回数据项中的经纬度信息
    autoSetFitView: {
      type: Boolean,
      default: true
    },
    // 是否在绘制后自动调整地图视野以适合全部点，默认true
    topAdcodes: {
      type: Array
    },
    // 顶层区划的adcode列表
    excludedAdcodes: {
      type: Array
    },
    renderOptions: {
      type: Object
    }
    // 绘制的引擎的参数
  },
  data() {
    return {};
  },
  methods: {
    __initComponent(options) {
      options.map = this.$parentComponent;
      this.$amapComponent = new districtCluster.DistrictCluster(options);
    },
    destroyComponent() {
      this.$amapComponent.destroy();
      this.$amapComponent = null;
      this.$parentComponent = null;
    }
  },
  render() {
    return null;
  }
});

exports.default = script;
//# sourceMappingURL=DistrictCluster.vue2.js.map
